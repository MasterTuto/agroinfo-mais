import React from 'react';
import { View } from 'react-native';

export default function Separator() {
	return (
		<View style={{height: 1, backgroundColor: '#3c3c3c', width: 300, alignSelf: 'center'}}>
		</View>
	);
}